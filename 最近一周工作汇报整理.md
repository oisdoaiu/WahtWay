# WahtWay 最近一周工作汇报整理

汇报时间建议：2026 年 7 月 27 日  
统计范围：主要参考 2026 年 7 月 20 日至 2026 年 7 月 26 日的提交记录，另包含 7 月 19 日的一条边界修复提交。  
项目定位：WahtWay 是一个面向大学生的本地 AI Agent 桌面应用，支持多模型对话、Skill 自动匹配、本地文件/命令/PPT 工具、外部 API 工具、MCP Server 接入，以及独立的 Skill Hub。

## 一、汇报时先讲的总括

这一周我们组的工作重点，可以概括为一句话：

> 我们把 WahtWay 从“能对话、能用本地 Skill 和工具”的应用，推进到了“能接入 MCP 生态、能管理工具权限、能感知和修复 Skill 依赖、能自动打包部署”的阶段。

从提交记录看，最近一周共有约 120 条提交，涉及 128 个文件，代码增量约 2.2 万行。主要改动集中在 `client/be/src/mcp/`、`client/be/src/skills/`、`client/src/App.tsx`、`client/src/McpPanel.tsx`、`.github/workflows/`、`server/` 和 `mcp-servers/ppt-server/`。

## 二、本周完成的核心工作

### 1. MCP Server 接入能力

本周完成了 MCP Connector 的主体能力。现在客户端可以配置本地 stdio MCP Server，并通过后端启动、停止、重启、测试连接和发现工具。

可以汇报的点：

- 支持保存 MCP Server 配置，包括启动命令、参数、工作目录、环境变量、是否启用、是否自动启动等。
- 后端通过 MCP TypeScript SDK 管理 stdio runtime，完成初始化握手、工具列表发现和工具注册。
- MCP 工具会进入 WahtWay 原有 Agent Tool Registry，Agent 可以像调用内置工具一样调用 MCP 工具。
- 前端新增了 MCP 管理页面，用于展示 server 状态、工具列表、错误信息和操作按钮。
- 加了 MCP runtime 相关测试，覆盖 server 生命周期、异常退出、健康检查和重连恢复。

涉及文件示例：

- `client/be/src/mcp/runtime.ts`
- `client/be/src/mcp/repository.ts`
- `client/be/src/routes/mcp.ts`
- `client/src/McpPanel.tsx`
- `client/MCP_DESIGN.md`

汇报表述建议：

> 这一部分解决的是 WahtWay 的扩展性问题。以前工具主要由我们自己写死在项目里，现在可以通过 MCP 接入外部工具服务，后续别人写的工具也能以标准协议接入我们的 Agent。

### 2. MCP 工具权限、安全元数据和审计

在 MCP 接入之后，本周继续补上了权限和安全治理。因为 MCP Server 本质上是本地可执行程序，工具可能涉及读、写、删除、网络访问等风险，所以我们做了更细粒度的控制。

可以汇报的点：

- 将旧的 server 级 `requireApproval` 升级为工具级权限策略。
- 每个 MCP 工具支持三种权限：自动调用、每次确认、禁用。
- 支持按工具设置风险类型，例如只读、写入、破坏性操作。
- 禁用的工具不会注册进 Agent Tool Registry，模型无法调用。
- 支持 MCP 工具列表变化通知，比如 server 更新后新增、删除或修改工具。
- 记录工具列表刷新、权限修改、安全策略修改的审计历史，便于回溯。

涉及文件示例：

- `client/MCP_TOOL_PERMISSIONS.md`
- `client/MCP_TOOL_SAFETY_DESIGN.md`
- `client/MCP_TOOL_CHANGE_AUDIT_DESIGN.md`
- `client/be/src/mcp/tool-change-audit.ts`
- `client/be/src/mcp/tool-change-audit.test.ts`

汇报表述建议：

> 我们不是只把 MCP 跑起来，而是同步考虑了“工具能不能被安全地交给 Agent 使用”。比如危险工具默认不直接自动执行，禁用工具从注册表里消失，权限变化也会留下审计记录。

### 3. Skill 与 MCP 工具绑定、依赖健康检查和修复闭环

本周另一个重点是把 MCP 工具和 Skill 系统关联起来。现在 Skill 可以声明自己依赖哪些 MCP 工具，系统会检查这些依赖是否可用，并在不可用时阻止运行或引导修复。

可以汇报的点：

- Skill manifest 新增 MCP binding 信息，可以把某个 Skill 绑定到指定 MCP server 和 tool。
- 支持从 MCP 工具创建 Skill assistant，让 MCP 工具更容易被用户以“技能模式”使用。
- 后端暴露 Skill 依赖健康检查 API，判断依赖状态是正常、降级还是不可用。
- 当前端发现 Skill 依赖异常时，会显示异常原因和修复入口。
- 新增依赖修复服务：可以为失效 binding 查询候选工具，预览变更 diff，用户确认后再写入 Skill manifest。
- 修复记录会被审计保存，避免用户不知道系统改了什么。
- 针对依赖状态、修复一致性、修复审计都补充了测试。

涉及文件示例：

- `client/be/src/skills/dependency-health.ts`
- `client/be/src/skills/dependency-repair.ts`
- `client/be/src/skills/dependency-repair-audit.ts`
- `client/be/src/routes/skills.ts`
- `client/MCP_SKILL_DEPENDENCY_HEALTH.md`
- `client/MCP_SKILL_DEPENDENCY_REPAIR.md`

汇报表述建议：

> 这部分可以理解为给 Skill 加了“依赖体检”。如果某个 Skill 依赖的 MCP 工具没启动、被禁用、名字变化或 server 不存在，系统不会让它静默失败，而是明确告诉用户原因，并给出可确认的修复流程。

### 4. Skill 体系扩展：模式切换、外部导入、格式规范

除了 MCP，本周也完善了 Skill 本身的使用体验和生态接入。

可以汇报的点：

- 聊天页新增 Skill 模式切换器，可以在智能模式和指定 Skill 模式之间切换。
- Skill 可以配置分类、颜色、图标、欢迎语、示例问题，让 Skill 更像一个可选择的工作模式。
- 支持将模式信息发布到 Skill Hub。
- 支持外部 Skill 导入：URL 导入、GitHub 仓库扫描、批量安装。
- GitHub Skill 仓库扫描支持用 LLM 生成中文摘要，降低用户理解成本。
- 新增统一 Skill Manifest v1 规范和 JSON Schema 校验脚本，为后续 Skill Hub 和外部导入打基础。

涉及文件示例：

- `client/src/App.tsx`
- `client/be/src/skills/converter.ts`
- `schemas/wahtway-skill-manifest-v1.schema.json`
- `scripts/validate-skill-format.mjs`
- `docs/Skill统一格式规范.md`

汇报表述建议：

> 以前 Skill 更像后台能力，现在我们把它产品化成了“模式”。用户可以直接选择“日报整理”“代码解释”“PPT 制作”等模式，也可以从外部仓库或 Hub 导入新的 Skill。

### 5. AI 配置、本地记忆和上下文压缩

本周也补齐了多模型配置、用户记忆和长对话能力。

可以汇报的点：

- 支持本地 AI Provider 设置，包括 DeepSeek、OpenAI、通义千问、智谱、Moonshot、SiliconFlow 和自定义 OpenAI 兼容接口。
- 支持模型列表和 API 设置在本地保存。
- 新增自动用户画像提取能力，可以从对话中提取偏好、身份、项目、长期指令等候选记忆。
- 新增上下文压缩机制，用 rolling summary 减少长对话 token 压力。
- 聊天流程支持对话历史、摘要和记忆共同进入上下文构建。

涉及文件示例：

- `client/be/src/ai-settings.ts`
- `client/be/src/routes/ai-config.ts`
- `client/be/src/memory/profile-extractor.ts`
- `client/be/src/context/compactor.ts`
- `client/be/src/context/builder.ts`
- `client/MEMORY_DESIGN.md`
- `docs/context-compaction-plan.md`

汇报表述建议：

> 这部分主要提升长期使用体验。用户不需要每次重新配置模型，也不用在长对话里反复交代背景；系统可以保存配置、压缩历史，并沉淀可控的用户记忆。

### 6. Agent 工具审批恢复

本周对 Agentic Loop 做了可恢复改造，解决工具审批后回复中断的问题。

可以汇报的点：

- Agent 工具调用遇到需要用户确认时，会保存 pending run。
- 用户确认后，Agent 可以从中断点继续执行，而不是重新发起整轮对话。
- 支持多路径工具审批隔离，避免不同对话或不同工具调用互相串扰。
- 新增审批恢复 API 和测试。

涉及文件示例：

- `client/be/src/agent-runs/repository.ts`
- `client/be/src/agent-runs/approval.ts`
- `client/be/src/routes/agent-runs.ts`
- `client/AGENT_APPROVAL_RESUME_DESIGN.md`

汇报表述建议：

> 这解决的是 Agent 使用工具时的交互连续性。需要确认的工具不会让整轮回答断掉，确认后系统能接着跑，这是从 demo 原型走向真实可用应用的重要一步。

### 7. PPT 能力和 PPT MCP Server

本周新增了 PPT 相关工具和一个基于 Python 的 PPT MCP Server。

可以汇报的点：

- 后端新增 `pptx-tools.ts`，支持 PPT 相关能力打包到桌面端。
- 新增 `mcp-servers/ppt-server/server.py`，基于 `python-pptx` 暴露 PPT 工具。
- PPT MCP Server 设计了读取 PPT 结构、克隆填充、创建 PPT 等工具能力。
- 这为“让 Agent 帮用户生成或修改汇报 PPT”提供了原型基础。

涉及文件示例：

- `client/be/src/tools/pptx-tools.ts`
- `client/be/data/skills/ppt-maker.json`
- `mcp-servers/ppt-server/server.py`

汇报表述建议：

> 这个功能适合现场展示，因为和我们明天汇报场景很贴近：WahtWay 不只是聊天，还能接入一个 PPT MCP Server，让 Agent 拥有读写 PPT 的工具能力。

### 8. Skill Hub、安全和部署自动化

本周也推进了独立 Skill Hub 和工程发布能力。

可以汇报的点：

- Skill Hub 管理接口增加鉴权保护，避免管理 API 裸露。
- 支持可编辑的匿名 Skill 评分。
- 新增 Skill Hub invite deployment。
- GitHub Actions 增加 Electron 多平台打包流程和 Skill Hub 定时部署流程。
- 修复 Skill Hub 健康检查等待问题，提升自动化部署稳定性。

涉及文件示例：

- `server/src/auth/`
- `server/src/routes/auth.ts`
- `server/src/routes/skills.ts`
- `.github/workflows/build-electron.yml`
- `.github/workflows/deploy-skill-hub.yml`

汇报表述建议：

> 我们这一周不仅写功能，也把发布链路补起来了。桌面端可以通过 CI 自动打包，Skill Hub 也有部署流程，方便后续持续迭代和演示。

### 9. UI 和体验修复

本周还有一些体验层面的收尾工作。

可以汇报的点：

- 修复深色模式覆盖不完整的问题。
- 调整侧边栏导航图标对齐。
- 修复 MCP 页面和 Skill 页面内容串页问题。
- 修复移动端 MCP 修复流程超出视口问题。
- 修复 Skill 编辑时 system prompt 被覆盖的问题。
- 修复工作区路径清空和 Skill Hub 模型发现等问题。

涉及文件示例：

- `client/src/App.css`
- `client/src/App.tsx`
- `client/src/McpPanel.tsx`

汇报表述建议：

> 这些改动虽然不一定是大功能，但会直接影响老师现场看系统时的稳定感，比如暗色模式、侧边栏、移动端弹窗、Skill 编辑保存等。

## 三、建议明天重点展示什么

建议不要把所有提交都讲一遍，而是按“用户能看到的链路”展示 4 个核心场景。

### 展示 1：整体应用入口和聊天模式

展示目标：说明 WahtWay 是一个本地 AI Agent 桌面/网页应用，不是普通聊天页面。

操作建议：

1. 启动后打开 `http://localhost:5173` 或 Electron 客户端。
2. 展示侧边栏、聊天主界面、AI 配置入口。
3. 展示顶部或聊天区的 Skill 模式切换器。
4. 从“智能模式”切到一个具体 Skill，比如代码解释、日报整理、PPT 制作。

讲解重点：

- 多模型配置已经本地化。
- Skill 不只是后台 prompt，而是用户可选择的工作模式。
- Agent 可以根据任务选择工具和 Skill。

### 展示 2：MCP Server 管理和工具权限

展示目标：证明系统已经具备外部工具扩展能力和安全控制。

操作建议：

1. 打开 MCP 管理页。
2. 展示 MCP Server 列表。
3. 展示某个 Server 的启动命令、参数、状态、工具列表。
4. 展示工具权限下拉：自动调用、每次确认、禁用。
5. 如果有可用示例 server，可以点击测试或启动，并展示工具发现结果。

讲解重点：

- MCP 是标准化工具协议，后续外部工具可以标准接入。
- 我们实现了工具级权限，不是简单粗暴全开。
- 禁用工具不会进入 Agent 可调用列表，安全策略在后端强制执行。

### 展示 3：Skill 依赖健康检查和修复

展示目标：展示本周最有技术含量的一条闭环。

操作建议：

1. 打开 Skill 页面或 MCP 页面中与 Skill 绑定相关的区域。
2. 展示 Skill 依赖状态：正常、部分受限、不可用。
3. 如果能准备一个异常例子，可以临时停掉某个 MCP Server 或禁用某个工具。
4. 展示系统提示依赖异常原因。
5. 点击修复入口，展示候选工具、预览 diff、确认修复流程。

讲解重点：

- Skill 运行前会检查依赖，不会等失败后才报错。
- 修复不是自动乱改，而是候选推荐、diff 预览、用户确认、审计记录。
- 这体现了我们对 Agent 工具生态可靠性的考虑。

### 展示 4：PPT MCP Server 或 PPT Skill

展示目标：把技术能力落到学生实际场景。

操作建议：

1. 展示 `ppt-maker` Skill 或 PPT 相关工具。
2. 展示 `mcp-servers/ppt-server/server.py` 的工具能力说明。
3. 如果环境准备充分，可以让 Agent 读取一个 PPT 结构或生成一份简单 PPT。
4. 如果现场环境不稳定，可以只展示工具列表和代码结构，不强行现场生成。

讲解重点：

- 这个场景贴近课程汇报、简历展示、学习总结等学生需求。
- PPT 能力通过工具/MCP 接入，不需要把所有逻辑写死在聊天模型里。

## 四、建议的汇报顺序

建议控制在 6 到 10 分钟。

1. 项目一句话介绍，30 秒。
   - WahtWay 是面向大学生的本地 AI Agent 桌面应用。
   - 支持 Skill、工具调用、MCP 接入、Skill Hub。

2. 本周总体进展，1 分钟。
   - MCP 接入。
   - 工具权限和安全审计。
   - Skill-MCP 依赖健康与修复。
   - Skill 生态导入和模式切换。
   - AI 配置、记忆、上下文压缩。
   - CI 打包和 Hub 部署。

3. 核心技术亮点，2 到 3 分钟。
   - MCP runtime 和工具注册。
   - 工具级权限与风险控制。
   - Skill 依赖健康检查和修复闭环。
   - Agent 审批后可恢复执行。

4. 现场演示，3 到 4 分钟。
   - 切换 Skill 模式。
   - 打开 MCP 页面看工具。
   - 展示权限控制。
   - 展示依赖健康或 PPT 工具。

5. 总结和下一步，1 分钟。
   - 当前已经具备可扩展 Agent 平台雏形。
   - 下一步可以继续做多格式文件解析、Skill 社区、自动更新、更多 MCP Server。

## 五、可以准备的展示材料

建议提前准备这些内容，避免现场临时找：

- 一张整体架构图：React/Vite 前端、Electron、嵌入式 Express API、MCP Runtime、Skill Hub。
- 一张本周功能列表截图：可以从本文件第二部分摘取。
- MCP 管理页面截图：重点展示 server 状态和工具权限。
- Skill 模式切换截图：展示“智能模式”和多个 Skill。
- Skill 依赖异常/修复截图：如果能构造异常，截图价值很高。
- GitHub Actions 截图：展示 Electron 打包和 Skill Hub 部署流程。
- 提交记录截图：展示本周持续集成开发，而不是最后一天集中堆代码。

## 六、建议现场命令

如果现场用浏览器开发模式演示：

```powershell
cd client\be
npm run dev
```

另开一个终端：

```powershell
cd client
npm run dev
```

访问：

```text
http://localhost:5173
```

如果现场用 Electron 演示：

```powershell
cd client
npm run build
npm run electron
```

如果要展示 Skill Hub：

```powershell
cd server
npm run dev
```

访问：

```text
http://localhost:4000
```

## 七、可能被老师问到的问题和回答

### Q1：你们为什么要接 MCP？

回答建议：

> 因为 MCP 可以让工具接入标准化。我们不需要把所有工具都写在主项目里，只要外部工具按 MCP 协议暴露出来，WahtWay 就能发现并注册给 Agent 使用。这样系统扩展性更强，也更接近真实 AI Agent 应用的生态模式。

### Q2：MCP 工具会不会有安全风险？

回答建议：

> 会有，所以我们没有简单地全部自动执行。本周我们做了工具级权限、风险元数据、禁用工具不注册、每次调用确认、权限变更审计等机制。关键策略由后端执行，前端只是展示和配置，避免绕过。

### Q3：Skill 和 MCP 是什么关系？

回答建议：

> Skill 是用户可选择的任务模式，MCP 是工具接入协议。一个 Skill 可以声明它依赖哪些 MCP 工具，比如 PPT 制作 Skill 依赖 PPT MCP Server。系统会在运行前检查这些工具是否可用，不可用时会提示并引导修复。

### Q4：和普通 AI 聊天页面相比，你们的差异是什么？

回答建议：

> 普通聊天主要靠模型文本回答。WahtWay 的重点是本地 Agent：它有 Skill 模式、能调用文件和命令工具、能接外部 API 和 MCP Server、能管理权限、能保存本地记忆，还能通过 Skill Hub 扩展能力。

### Q5：这周工作最大的技术难点是什么？

回答建议：

> 最大难点是 MCP 工具接入后的可靠性和安全闭环。工具会动态变化，权限也会影响 Agent 能否调用，Skill 还可能依赖某个具体工具。所以我们做了 runtime 健康检查、工具列表变化审计、Skill 依赖健康检查、修复候选和 diff 确认，确保系统不会静默失败。

### Q6：下一步准备做什么？

回答建议：

> 下一步可以继续完善三个方向：第一是多格式文件能力，比如 PDF、Word、PPT 更完整的解析和生成；第二是 Skill 社区能力，比如评分、评论、排行、Fork；第三是桌面端自动更新和更多 MCP Server 模板，让普通用户更容易安装和使用。

## 八、提交记录可作为证据的主题

可以在汇报最后展示提交记录，说明本周是持续推进而不是一次性补材料。

代表性提交主题：

- 7 月 20 日：MCP Connector MVP、MCP Server 生命周期 API、MCP 管理 UI、工具权限策略。
- 7 月 21 日：Agent 工具审批恢复、PPT 工具合并、工作区路径修复。
- 7 月 22 日：MCP runtime 健康检查和重连、工具列表变化通知、多 AI Provider 设置。
- 7 月 23 日：MCP 工具变更审计、Skill Hub 修复、工作区支持、模型/API 选择器。
- 7 月 24 日：工具安全元数据、Skill 工具选择器、用户画像提取、上下文压缩。
- 7 月 25 日：Skill 模式切换、外部 Skill 导入、GitHub 仓库扫描、PPT MCP Server、Electron 自动打包、Skill Hub 部署。
- 7 月 26 日：Skill-MCP 依赖修复 API、修复服务、修复审计、前端修复控件、深色模式和导航体验修复。

## 九、建议汇报 PPT 大纲

如果要做 PPT，建议 8 页左右：

1. 项目介绍：WahtWay 是什么，解决什么问题。
2. 本周总览：一句话总结 + 六个关键词。
3. 架构图：Client、Embedded API、MCP Runtime、Skill Hub。
4. MCP 接入：Server 配置、工具发现、Agent 调用。
5. 安全治理：权限、风险、审计、确认机制。
6. Skill 生态：模式切换、外部导入、依赖健康、修复闭环。
7. 现场演示：列出演示步骤和备用截图。
8. 总结与下一步：已完成、价值、后续计划。

## 十、最终总结话术

可以用这一段作为汇报收尾：

> 本周我们主要完成了 WahtWay 的扩展能力和可靠性建设。MCP 让外部工具可以标准化接入，工具权限和审计保证调用过程可控，Skill 依赖健康检查和修复让用户知道问题在哪里、怎么修。再加上多模型配置、记忆、上下文压缩、PPT 工具和 CI 打包部署，WahtWay 已经从一个本地 AI 对话工具，进一步变成了一个可扩展、可演示、可持续迭代的本地 Agent 平台。

